{
  "__inputs": [],
  "__requires": [],
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 1,
  "id": null,
  "links": [],
  "panels": [
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 0 },
      "id": 100,
      "title": "CPU",
      "type": "row"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${datasource}" },
      "fieldConfig": {
        "defaults": {
          "custom": { "lineWidth": 2 },
          "unit": "percentunit"
        },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 1 },
      "id": 1,
      "options": { "legend": { "calcs": ["mean", "max"], "displayMode": "table", "placement": "bottom" }, "tooltip": { "mode": "multi" } },
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "expr": "rate(process_cpu_seconds_total{job=\"tempo-blockpack\"}[1m])",
          "legendFormat": "blockpack"
        },
        {
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "expr": "rate(process_cpu_seconds_total{job=\"tempo-parquet\"}[1m])",
          "legendFormat": "parquet"
        }
      ],
      "title": "CPU Usage",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${datasource}" },
      "fieldConfig": {
        "defaults": {
          "custom": { "lineWidth": 2 },
          "unit": "short"
        },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 1 },
      "id": 2,
      "options": { "legend": { "calcs": ["mean", "max"], "displayMode": "table", "placement": "bottom" }, "tooltip": { "mode": "multi" } },
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "expr": "go_goroutines{job=\"tempo-blockpack\"}",
          "legendFormat": "blockpack"
        },
        {
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "expr": "go_goroutines{job=\"tempo-parquet\"}",
          "legendFormat": "parquet"
        }
      ],
      "title": "Goroutines",
      "type": "timeseries"
    },
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 9 },
      "id": 101,
      "title": "Memory",
      "type": "row"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${datasource}" },
      "fieldConfig": {
        "defaults": {
          "custom": { "lineWidth": 2 },
          "unit": "bytes"
        },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 10 },
      "id": 3,
      "options": { "legend": { "calcs": ["mean", "max"], "displayMode": "table", "placement": "bottom" }, "tooltip": { "mode": "multi" } },
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "expr": "process_resident_memory_bytes{job=\"tempo-blockpack\"}",
          "legendFormat": "blockpack RSS"
        },
        {
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "expr": "process_resident_memory_bytes{job=\"tempo-parquet\"}",
          "legendFormat": "parquet RSS"
        }
      ],
      "title": "RSS Memory",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${datasource}" },
      "fieldConfig": {
        "defaults": {
          "custom": { "lineWidth": 2 },
          "unit": "bytes"
        },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 10 },
      "id": 4,
      "options": { "legend": { "calcs": ["mean", "max"], "displayMode": "table", "placement": "bottom" }, "tooltip": { "mode": "multi" } },
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "expr": "go_memstats_heap_inuse_bytes{job=\"tempo-blockpack\"}",
          "legendFormat": "blockpack heap in-use"
        },
        {
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "expr": "go_memstats_heap_inuse_bytes{job=\"tempo-parquet\"}",
          "legendFormat": "parquet heap in-use"
        },
        {
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "expr": "go_memstats_heap_alloc_bytes{job=\"tempo-blockpack\"}",
          "legendFormat": "blockpack heap alloc"
        },
        {
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "expr": "go_memstats_heap_alloc_bytes{job=\"tempo-parquet\"}",
          "legendFormat": "parquet heap alloc"
        }
      ],
      "title": "Heap Memory",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${datasource}" },
      "fieldConfig": {
        "defaults": {
          "custom": { "lineWidth": 2 },
          "unit": "Bps"
        },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 18 },
      "id": 5,
      "options": { "legend": { "calcs": ["mean", "max"], "displayMode": "table", "placement": "bottom" }, "tooltip": { "mode": "multi" } },
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "expr": "rate(go_memstats_alloc_bytes_total{job=\"tempo-blockpack\"}[1m])",
          "legendFormat": "blockpack"
        },
        {
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "expr": "rate(go_memstats_alloc_bytes_total{job=\"tempo-parquet\"}[1m])",
          "legendFormat": "parquet"
        }
      ],
      "title": "Heap Allocation Rate",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${datasource}" },
      "fieldConfig": {
        "defaults": {
          "custom": { "lineWidth": 2 },
          "unit": "short"
        },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 18 },
      "id": 6,
      "options": { "legend": { "calcs": ["mean", "max"], "displayMode": "table", "placement": "bottom" }, "tooltip": { "mode": "multi" } },
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "expr": "go_memstats_stack_inuse_bytes{job=\"tempo-blockpack\"}",
          "legendFormat": "blockpack stack"
        },
        {
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "expr": "go_memstats_stack_inuse_bytes{job=\"tempo-parquet\"}",
          "legendFormat": "parquet stack"
        },
        {
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "expr": "go_memstats_mspan_inuse_bytes{job=\"tempo-blockpack\"}",
          "legendFormat": "blockpack mspan"
        },
        {
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "expr": "go_memstats_mspan_inuse_bytes{job=\"tempo-parquet\"}",
          "legendFormat": "parquet mspan"
        }
      ],
      "title": "Stack & Metadata Memory",
      "type": "timeseries"
    },
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 26 },
      "id": 102,
      "title": "Go GC",
      "type": "row"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${datasource}" },
      "fieldConfig": {
        "defaults": {
          "custom": { "lineWidth": 2 },
          "unit": "s"
        },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 27 },
      "id": 7,
      "options": { "legend": { "calcs": ["mean", "max"], "displayMode": "table", "placement": "bottom" }, "tooltip": { "mode": "multi" } },
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "expr": "go_gc_duration_seconds{job=\"tempo-blockpack\", quantile=\"1\"}",
          "legendFormat": "blockpack p100"
        },
        {
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "expr": "go_gc_duration_seconds{job=\"tempo-blockpack\", quantile=\"0.75\"}",
          "legendFormat": "blockpack p75"
        },
        {
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "expr": "go_gc_duration_seconds{job=\"tempo-parquet\", quantile=\"1\"}",
          "legendFormat": "parquet p100"
        },
        {
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "expr": "go_gc_duration_seconds{job=\"tempo-parquet\", quantile=\"0.75\"}",
          "legendFormat": "parquet p75"
        }
      ],
      "title": "GC Pause Duration",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${datasource}" },
      "fieldConfig": {
        "defaults": {
          "custom": { "lineWidth": 2 },
          "unit": "percentunit"
        },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 27 },
      "id": 8,
      "options": { "legend": { "calcs": ["mean", "max"], "displayMode": "table", "placement": "bottom" }, "tooltip": { "mode": "multi" } },
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "expr": "go_memstats_gc_cpu_fraction{job=\"tempo-blockpack\"}",
          "legendFormat": "blockpack"
        },
        {
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "expr": "go_memstats_gc_cpu_fraction{job=\"tempo-parquet\"}",
          "legendFormat": "parquet"
        }
      ],
      "title": "GC CPU Fraction",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${datasource}" },
      "fieldConfig": {
        "defaults": {
          "custom": { "lineWidth": 2 },
          "unit": "ops"
        },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 35 },
      "id": 9,
      "options": { "legend": { "calcs": ["mean", "max"], "displayMode": "table", "placement": "bottom" }, "tooltip": { "mode": "multi" } },
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "expr": "rate(go_gc_duration_seconds_count{job=\"tempo-blockpack\"}[1m])",
          "legendFormat": "blockpack"
        },
        {
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "expr": "rate(go_gc_duration_seconds_count{job=\"tempo-parquet\"}[1m])",
          "legendFormat": "parquet"
        }
      ],
      "title": "GC Cycles/sec",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${datasource}" },
      "fieldConfig": {
        "defaults": {
          "custom": { "lineWidth": 2 },
          "unit": "short"
        },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 35 },
      "id": 10,
      "options": { "legend": { "calcs": ["mean", "max"], "displayMode": "table", "placement": "bottom" }, "tooltip": { "mode": "multi" } },
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "expr": "go_memstats_next_gc_bytes{job=\"tempo-blockpack\"}",
          "legendFormat": "blockpack next GC target"
        },
        {
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "expr": "go_memstats_next_gc_bytes{job=\"tempo-parquet\"}",
          "legendFormat": "parquet next GC target"
        },
        {
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "expr": "go_memstats_heap_alloc_bytes{job=\"tempo-blockpack\"}",
          "legendFormat": "blockpack heap alloc"
        },
        {
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "unit": "bytes",
          "expr": "go_memstats_heap_alloc_bytes{job=\"tempo-parquet\"}",
          "legendFormat": "parquet heap alloc"
        }
      ],
      "title": "GC Heap Pressure",
      "type": "timeseries"
    },
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 43 },
      "id": 103,
      "title": "Latency",
      "type": "row"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${datasource}" },
      "fieldConfig": {
        "defaults": {
          "custom": { "lineWidth": 2 },
          "unit": "s"
        },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 44 },
      "id": 11,
      "options": { "legend": { "calcs": ["mean", "max"], "displayMode": "table", "placement": "bottom" }, "tooltip": { "mode": "multi" } },
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "expr": "histogram_quantile(0.99, rate(tempodb_backend_request_duration_seconds_bucket{job=\"tempo-blockpack\"}[1m]))",
          "legendFormat": "blockpack p99 {{operation}}"
        },
        {
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "expr": "histogram_quantile(0.99, rate(tempodb_backend_request_duration_seconds_bucket{job=\"tempo-parquet\"}[1m]))",
          "legendFormat": "parquet p99 {{operation}}"
        },
        {
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "expr": "histogram_quantile(0.50, rate(tempodb_backend_request_duration_seconds_bucket{job=\"tempo-blockpack\"}[1m]))",
          "legendFormat": "blockpack p50 {{operation}}"
        },
        {
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "expr": "histogram_quantile(0.50, rate(tempodb_backend_request_duration_seconds_bucket{job=\"tempo-parquet\"}[1m]))",
          "legendFormat": "parquet p50 {{operation}}"
        }
      ],
      "title": "Backend Request Latency (S3) — p50 / p99",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${datasource}" },
      "fieldConfig": {
        "defaults": {
          "custom": { "lineWidth": 2 },
          "unit": "reqps"
        },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 52 },
      "id": 12,
      "options": { "legend": { "calcs": ["mean", "max"], "displayMode": "table", "placement": "bottom" }, "tooltip": { "mode": "multi" } },
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "expr": "rate(tempodb_backend_request_duration_seconds_count{job=\"tempo-blockpack\"}[1m])",
          "legendFormat": "blockpack {{operation}}"
        },
        {
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "expr": "rate(tempodb_backend_request_duration_seconds_count{job=\"tempo-parquet\"}[1m])",
          "legendFormat": "parquet {{operation}}"
        }
      ],
      "title": "Backend Request Rate",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "${datasource}" },
      "fieldConfig": {
        "defaults": {
          "custom": { "lineWidth": 2 },
          "unit": "s"
        },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 52 },
      "id": 13,
      "options": { "legend": { "calcs": ["mean", "max"], "displayMode": "table", "placement": "bottom" }, "tooltip": { "mode": "multi" } },
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "expr": "histogram_quantile(0.99, rate(tempodb_blocklist_poll_duration_seconds_bucket{job=\"tempo-blockpack\"}[1m]))",
          "legendFormat": "blockpack blocklist poll p99"
        },
        {
          "datasource": { "type": "prometheus", "uid": "${datasource}" },
          "expr": "histogram_quantile(0.99, rate(tempodb_blocklist_poll_duration_seconds_bucket{job=\"tempo-parquet\"}[1m]))",
          "legendFormat": "parquet blocklist poll p99"
        }
      ],
      "title": "Blocklist Poll Latency p99",
      "type": "timeseries"
    }
  ],
  "refresh": "15s",
  "schemaVersion": 38,
  "tags": ["tempo", "blockpack", "parquet", "comparison"],
  "templating": {
    "list": [
      {
        "current": {},
        "hide": 0,
        "includeAll": false,
        "name": "datasource",
        "options": [],
        "query": "prometheus",
        "refresh": 1,
        "type": "datasource",
        "label": "Prometheus"
      }
    ]
  },
  "time": { "from": "now-30m", "to": "now" },
  "timepicker": {},
  "timezone": "browser",
  "title": "Blockpack vs Parquet",
  "uid": "blockpack-vs-parquet",
  "version": 1
}
